name: Node.js CI

on:
  push:
    branches: [ "main" ]
jobs:
  build:
    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [22.x]

    steps: 
    
    - name: backup .env file 
      run: |
        if [ -f backend/.env ]; then mv backend/.env /tmp/env_backup; fi


    - uses: actions/checkout@v4
    - name: restore .env file 
      run: |
        if [ -f /tmp/env_backup ]; then mv /tmp/env_backup backend/.env; fi

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json
    - run: cd backend && npm ci
    - run: cd backend && npm run build --if-present
